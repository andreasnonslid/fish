# Fish color definitions

set -g RED (set_color red)
set -g GREEN (set_color green)
set -g YELLOW (set_color yellow)
set -g BLUE (set_color blue)
set -g CYAN (set_color cyan)
set -g WHITE (set_color white)
set -g LIGHT_RED (set_color brred)
set -g LIGHT_GREEN (set_color brgreen)
set -g LIGHT_YELLOW (set_color bryellow)
set -g LIGHT_BLUE (set_color brblue)
set -g NC (set_color normal)
